- if can? :create, News
  ul.buttons[style="float: right;"]
    li
      = link_to 'News bearbeiten', edit_news_path(@news), class: 'edit' if can? :update, Event
    li
      = link_to 'News löschen', news_path(@news), method: :delete, confirm: t(:really_delete?), class: 'delete' if can? :destroy, News
    li
      = link_to 'News erfassen', new_news_path, class: 'new' if can? :create, News
h2
  = @news.title
span.meta
  = l @news.published_at
  |  von 
  = @news.creator.try(:member) || "?"
div
  = @news.to_html
- content_for :sidebar do
  = render 'sidebar'
