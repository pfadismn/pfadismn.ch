<%= semantic_form_for @user, url: update_password_user_path(@user) do |f| %>
  <%= f.error_messages %>

  <%= f.inputs t(:password) do %>
    <%= f.input :email if @token %>
    <%= f.input :password %>
    <%= f.input :password_confirmation %>
  <% end %>

  <%= f.actions %>
<% end %>